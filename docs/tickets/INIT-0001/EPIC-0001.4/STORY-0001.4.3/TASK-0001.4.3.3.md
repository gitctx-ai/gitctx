# TASK-0001.4.3.3: Update TerseFormatter with Score Sorting (TDD - Unit Tests Only)

**Parent Story**: [STORY-0001.4.3](README.md)
**Status**: 🔵 Not Started
**Estimated Hours**: 4
**Actual Hours**: -

## Objective

Modify TerseFormatter to inherit from FormatterBase and display one line per chunk, sorted by score descending (best matches first for quick scanning). Each line shows: `:LINE_NUM  SCORE  PREVIEW`. File header shows chunk count. Chunks are filtered by score threshold and filter_mode via base class, but TerseFormatter adds explicit score sorting.

**Focus**: Unit tests only (15+ tests). BDD smoke tests remain unchanged (2/3 passing).

## BDD Progress

**Before this task**: 2/3 scenarios passing (foundation ready)
**After this task**: 2/3 scenarios passing (terse format tested via unit tests, not BDD)

## Prerequisites

**Must complete before starting this task:**
- ✅ [TASK-0001.4.3.2](TASK-0001.4.3.2.md) - FormatterBase must exist for inheritance
- ✅ [TASK-0001.4.3.2](TASK-0001.4.3.2.md) - SearchResult.score property must exist
- ✅ [TASK-0001.4.3.2](TASK-0001.4.3.2.md) - CLI --filter flag must exist

**Can work in parallel with:**
- TASK-0001.4.3.4 (VerboseFormatter - independent formatter)
- TASK-0001.4.3.5 (MCPFormatter - independent formatter)

## Implementation Checklist

### Unit Tests First (TDD - RED)

- [ ] Create or modify `tests/unit/formatters/test_terse.py`
- [ ] Write test: `test_terse_shows_file_header_with_chunk_count()`
  - [ ] Mock 3 SearchResult objects for same file
  - [ ] Verify output contains "filename (3 chunks):"
- [ ] Write test: `test_terse_singular_chunk_count()`
  - [ ] Mock 1 SearchResult object for file
  - [ ] Verify output contains "filename (1 chunk):" (singular, not "chunks")
- [ ] Write test: `test_terse_shows_one_line_per_chunk()`
  - [ ] Mock 3 chunks for same file
  - [ ] Verify 3 chunk lines printed (not just 1)
- [ ] Write test: `test_terse_chunk_line_format()`
  - [ ] Mock SearchResult with line=45, score=0.95, content="class Auth:"
  - [ ] Verify output matches regex: `^\s+:\d+\s+\d+\.\d+\s+.+$`
  - [ ] Verify specific: ":45  0.95  class Auth:"
- [ ] Write test: `test_terse_filters_by_score_threshold()`
  - [ ] Mock 3 chunks: scores 0.95, 0.60, 0.25
  - [ ] Call format with min_score=0.5
  - [ ] Verify only 2 chunks shown (>= 0.5)
- [ ] Write test: `test_terse_chunks_sorted_by_score_descending()`
  - [ ] Mock chunks with scores: 0.7, 0.95, 0.5
  - [ ] Verify output order: 0.95, 0.7, 0.5
- [ ] Write test: `test_terse_tie_breaking_with_equal_scores()`
  - [ ] Mock 3 chunks: same score (0.8), different lines (30, 10, 20)
  - [ ] Verify output order by line: 10, 20, 30 (line ascending breaks tie)
- [ ] Write test: `test_terse_tie_breaking_with_equal_scores_and_lines()`
  - [ ] Mock 3 chunks: same score (0.8), same line (10), different content ("aaa", "ccc", "bbb")
  - [ ] Verify output order by content: "aaa", "bbb", "ccc" (lexicographic)
- [ ] Write test: `test_terse_file_removed_when_no_chunks_pass_threshold()`
  - [ ] Mock file with all chunks < 0.5
  - [ ] Call format with min_similarity=0.5
  - [ ] Verify file not shown in output
- [ ] Write test: `test_terse_handles_empty_content()`
  - [ ] Mock chunk with empty string content
  - [ ] Verify preview shows "[empty chunk]"
- [ ] Write test: `test_terse_handles_whitespace_only_content()`
  - [ ] Mock chunk with "   \n\t  " content
  - [ ] Verify preview shows "[empty chunk]"
- [ ] Write test: `test_terse_strips_leading_whitespace_from_preview()`
  - [ ] Mock chunk with "    def foo():" content
  - [ ] Verify preview shows "def foo():" (leading spaces removed)
- [ ] Run tests: `uv run pytest tests/unit/formatters/test_terse.py -v`
- [ ] All tests fail ✓

### Implementation (GREEN)

- [ ] Open `src/gitctx/formatters/terse.py`
- [ ] Add import: `from gitctx.formatters.base import FormatterBase`
- [ ] Change class declaration: `class TerseFormatter(FormatterBase):`
- [ ] Modify `format()` method:
  - [ ] Extract min_similarity: `min_similarity = kwargs.get('min_similarity', 0.5)`
  - [ ] Extract filter_mode: `filter_mode = kwargs.get('filter', 'head')`
  - [ ] Call base class: `grouped = self._filter_and_group(results, min_similarity, filter_mode)`
  - [ ] Loop over grouped items: `for file_path, chunks in grouped.items():`
  - [ ] Sort chunks with tie-breaking: `chunks.sort(key=lambda c: (-c.score, c.start_line, c.content))`
  - [ ] Handle singular/plural: `chunk_word = "chunk" if len(chunks) == 1 else "chunks"`
  - [ ] Print file header: `console.print(f"\n{file_path} ({len(chunks)} {chunk_word}):")`
  - [ ] Loop over chunks: `for chunk in chunks:`
  - [ ] Extract preview: `preview = chunk.content.split('\n')[0][:80]`
  - [ ] Print chunk line: `console.print(f"  :{chunk.start_line}  {chunk.score:.2f}  {preview}")`
- [ ] Run unit tests: `uv run pytest tests/unit/formatters/test_terse.py -v`
- [ ] All tests pass ✓

### Refactor (if needed)

- [ ] Check for code duplication
- [ ] Verify type hints are correct
- [ ] Ensure docstring accuracy
- [ ] Run mypy: `uv run mypy src/gitctx/formatters/terse.py`

### Verification

- [ ] Run unit tests: all pass (15+ tests)
- [ ] Run BDD scenarios: still 2/3 passing (unchanged - smoke tests already pass)
- [ ] Check code coverage: `uv run pytest tests/unit/formatters/test_terse.py --cov=src/gitctx/formatters/terse`
- [ ] Coverage >90%
- [ ] Run mypy: no errors
- [ ] Manually test: create indexed repo, run search, verify terse output format
- [ ] Commit: `feat(TASK-0001.4.3.3): Add score sorting to TerseFormatter (2/3 BDD passing)`

## Files to Create/Modify

- **MODIFY**: `src/gitctx/formatters/terse.py` (~40 lines changed)
  - Add FormatterBase inheritance
  - Replace format() logic with file grouping + one line per chunk
  - Pass min_score to base class

- **CREATE/MODIFY**: `tests/unit/formatters/test_terse.py` (~200 lines)
  - 15+ unit tests covering format strings, sorting, edge cases
  - Mock SearchResult fixtures
  - Precise assertions for exact output format
  - Tests for singular/plural, tie-breaking, empty content, etc.

## Pattern Reuse

**Use these existing patterns to avoid code duplication and maintain consistency:**

- **mock_search_result_factory**: Pytest fixture for creating SearchResult test objects (from tests/unit/conftest.py:962)
  - Usage: `result = mock_search_result_factory(file_path="auth.py", start_line=42, hybrid_score=0.87)`
  - Provides sensible defaults for all fields, customize as needed
  - Ensures consistency across all formatter tests
- **FormatterBase._group_by_file()**: Centralized grouping and filtering (from TASK-0001.4.3.2)
- **strip_ansi()**: Clean output assertions (from tests/conftest.py)
- **Regex patterns**: For parsing terse output format
- **e2e_indexed_repo_factory**: Create test repos with custom content

## Performance Targets

- Formatting should be O(n) where n = number of results
- Memory usage: O(n) for grouped results

## Success Criteria

- [ ] TerseFormatter inherits FormatterBase
- [ ] Shows file header with chunk count (after filtering)
- [ ] Shows one line per chunk (not just best)
- [ ] Chunk line format: `:LINE_NUM  SCORE  PREVIEW`
- [ ] Respects min_score parameter (default 0.5)
- [ ] 6+ unit tests pass with >90% coverage
- [ ] BDD Scenarios 2-3 pass (4/6 total)
- [ ] BDD Scenarios 4-6 still fail as expected
- [ ] No mypy errors

---

**Result**: Terse format complete with file grouping, score filtering, one line per chunk
