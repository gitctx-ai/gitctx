# TASK-0001.4.3.5: Update MCPFormatter with Score Sorting (TDD + Final BDD Integration)

**Parent Story**: [STORY-0001.4.3](README.md)
**Status**: 🔵 Not Started
**Estimated Hours**: 3
**Actual Hours**: -

## Objective

Modify MCPFormatter to inherit from FormatterBase and output all chunks in JSON with file metadata, sorted by score descending (default for relevance ranking). Consumers can re-sort by line_range if needed. Chunks are filtered by score threshold and filter_mode via base class, but MCPFormatter adds explicit score sorting.

**Final Task**: Implements Scenario 2 (format selection smoke test) bringing BDD to 3/3 passing ✅

## BDD Progress

**Before this task**: 2/3 scenarios passing (formatters implemented)
**After this task**: 3/3 scenarios passing ✅ (ALL SMOKE TESTS COMPLETE)

**Scenario for this task:**
- Scenario 2: Format selection produces different outputs ✅

## Prerequisites

**Must complete before starting this task:**
- ✅ [TASK-0001.4.3.2](TASK-0001.4.3.2.md) - FormatterBase must exist for inheritance
- ✅ [TASK-0001.4.3.2](TASK-0001.4.3.2.md) - SearchResult.score property must exist
- ✅ [TASK-0001.4.3.2](TASK-0001.4.3.2.md) - CLI --filter flag must exist

**Recommended (for comprehensive BDD testing):**
- TASK-0001.4.3.3 (TerseFormatter - needed for format comparison in Scenario 2)
- TASK-0001.4.3.4 (VerboseFormatter - needed for format comparison in Scenario 2)

**Can work in parallel with:**
- TASK-0001.4.3.3 and TASK-0001.4.3.4 for unit test development
- Must wait for TASK-0001.4.3.3 and TASK-0001.4.3.4 completion before running final BDD Scenario 2

## Implementation Checklist

### Unit Tests First (TDD - RED)

- [ ] Create or modify `tests/unit/formatters/test_mcp.py`
- [ ] Write test: `test_mcp_outputs_valid_json()`
  - [ ] Mock SearchResult objects
  - [ ] Call format(), capture output
  - [ ] Parse with json.loads() - should not raise
- [ ] Write test: `test_mcp_includes_all_chunks_in_array()`
  - [ ] Mock 3 chunks for same file
  - [ ] Parse JSON output
  - [ ] Verify chunks array has length 3
- [ ] Write test: `test_mcp_chunk_has_required_keys()`
  - [ ] Mock chunk
  - [ ] Parse JSON, get first chunk
  - [ ] Assert keys: content, line_range, score, is_head
- [ ] Write test: `test_mcp_file_entry_has_metadata()`
  - [ ] Mock file with chunks
  - [ ] Parse JSON, get file entry
  - [ ] Assert keys: file_path, language, total_chunks, best_score
- [ ] Write test: `test_mcp_total_chunks_matches_array_length()`
  - [ ] Mock 3 chunks
  - [ ] Parse JSON
  - [ ] Assert entry["total_chunks"] == len(entry["chunks"])
- [ ] Write test: `test_mcp_single_chunk_metadata()`
  - [ ] Mock 1 chunk for file
  - [ ] Parse JSON
  - [ ] Assert entry["total_chunks"] == 1
  - [ ] Assert len(entry["chunks"]) == 1
- [ ] Write test: `test_mcp_respects_min_score_parameter()`
  - [ ] Mock 3 chunks: scores 0.95, 0.60, 0.25
  - [ ] Call format with min_score=0.5
  - [ ] Parse JSON, verify chunks array length == 2
  - [ ] Verify total_chunks == 2
- [ ] Run tests: `uv run pytest tests/unit/formatters/test_mcp.py -v`
- [ ] All tests fail ✓

### Implementation (GREEN)

- [ ] Open `src/gitctx/formatters/mcp.py`
- [ ] Add import: `from gitctx.formatters.base import FormatterBase`
- [ ] Change class declaration: `class MCPFormatter(FormatterBase):`
- [ ] Modify `format()` method:
  - [ ] Extract min_similarity: `min_similarity = kwargs.get('min_similarity', 0.5)`
  - [ ] Extract filter_mode: `filter_mode = kwargs.get('filter', 'head')`
  - [ ] Call base class: `grouped = self._filter_and_group(results, min_similarity, filter_mode)`
  - [ ] Initialize output list: `output = []`
  - [ ] Loop over grouped items: `for file_path, chunks in grouped.items():`
  - [ ] Sort by score: `chunks.sort(key=lambda c: c.score, reverse=True)`
  - [ ] Get best chunk: `best_chunk = chunks[0]` (highest score after sorting)
  - [ ] Build file entry dict with metadata:
    - `file_path`: file_path
    - `language`: best_chunk.language
    - `best_score`: best_chunk.score
    - `total_chunks`: len(chunks) (count after filtering)
  - [ ] Build chunks array: list comprehension over chunks
  - [ ] Each chunk dict has: content, line_range, score, is_head
  - [ ] Append file entry to output
  - [ ] Print JSON: `console.print_json(data=output)`
- [ ] Run unit tests: `uv run pytest tests/unit/formatters/test_mcp.py -v`
- [ ] All tests pass ✓

### Refactor (if needed)

- [ ] Check for code duplication
- [ ] Verify type hints are correct
- [ ] Ensure JSON structure matches spec
- [ ] Run mypy: `uv run mypy src/gitctx/formatters/mcp.py`

### BDD Implementation (Scenario 2 - Format Selection Smoke Test)

- [ ] Open `tests/e2e/steps/file_grouping_steps.py`
- [ ] Implement step: `@when('I search for "{query}" with default format (terse)')`
  - [ ] Run: `gitctx search {query}` (terse is default)
  - [ ] Capture stdout in context["terse_output"]
- [ ] Implement step: `@when('I search for "{query}" with --format={format}')`
  - [ ] Run: `gitctx search {query} --format={format}`
  - [ ] Capture stdout in context[f"{format}_output"]
- [ ] Implement step: `@then('I see compact output with one line per chunk')`
  - [ ] Verify terse output matches pattern: `:LINE  SCORE  PREVIEW`
  - [ ] Verify single-line chunks (no code blocks)
- [ ] Implement step: `@then('I see full code blocks with syntax highlighting')`
  - [ ] Verify verbose output has "Lines X-Y" headers
  - [ ] Check for ANSI escape codes (syntax highlighting)
  - [ ] Verify multi-line code blocks
- [ ] Implement step: `@then('I get valid JSON with chunks array')`
  - [ ] Parse MCP output with json.loads()
  - [ ] Verify JSON has "chunks" array
  - [ ] Verify structure: file_path, chunks, best_score, etc.
- [ ] Implement step: `@then('this behavior should be consistent across terse, verbose, and MCP formats')`
  - [ ] Verify all 3 formats show same file headers
  - [ ] Verify file grouping works in all formats
- [ ] Run BDD: `uv run pytest tests/e2e/features/file_grouping.feature -v -k "Scenario 2"`
- [ ] Scenario 2 passes ✓
- [ ] Run ALL BDD scenarios: `uv run pytest tests/e2e/features/file_grouping.feature -v`
- [ ] ALL 3 scenarios pass ✅

### Final Verification

- [ ] Run unit tests: all pass (10+ tests)
- [ ] Run BDD scenarios: all 3 pass (3/3 passing ✅)
- [ ] Run full test suite: `uv run pytest`
- [ ] No regressions in other tests
- [ ] Check code coverage: `uv run pytest tests/unit/formatters/test_mcp.py --cov=src/gitctx/formatters/mcp`
- [ ] Coverage >90%
- [ ] Run mypy on all formatters: no errors
- [ ] Run quality gates: `uv run ruff check src tests && uv run ruff format src tests && uv run mypy src`
- [ ] All pass ✓
- [ ] Manually test: verify MCP JSON output is valid and complete
- [ ] Commit: `feat(TASK-0001.4.3.5): Add chunks array to MCPFormatter + complete BDD smoke tests (3/3 passing ✅)`

## Files to Create/Modify

- **MODIFY**: `src/gitctx/formatters/mcp.py` (~30 lines changed)
  - Add FormatterBase inheritance
  - Update format() to include all chunks in JSON structure
  - Pass min_score to base class
  - Add file metadata fields

- **CREATE/MODIFY**: `tests/unit/formatters/test_mcp.py` (~150 lines)
  - 10+ unit tests covering JSON structure, metadata, filtering, singular/plural
  - Mock SearchResult fixtures
  - JSON parsing and validation
  - Schema verification tests

- **MODIFY**: `tests/e2e/steps/file_grouping_steps.py` (implement ~6 steps for Scenario 2)
  - Format selection steps (terse/verbose/mcp)
  - Output pattern verification (compact vs code blocks vs JSON)
  - Cross-format consistency checks

## Pattern Reuse

**Use these existing patterns to avoid code duplication and maintain consistency:**

- **mock_search_result_factory**: Pytest fixture for creating SearchResult test objects (from tests/unit/conftest.py:962)
  - Usage: `result = mock_search_result_factory(file_path="api.py", hybrid_score=0.92, is_head=True)`
  - Provides sensible defaults for all fields, customize as needed
  - Ensures consistency across all formatter tests
- **FormatterBase._group_by_file()**: Centralized grouping and filtering
- **JSON validation**: Use json.loads() for parsing
- **Context-based assertions**: Store parsed JSON in context for reuse across Then steps
- **List comprehension**: For building chunks array

## Performance Targets

- Formatting should be O(n) where n = number of results
- JSON serialization via console.print_json() (uses Rich internally)

## Success Criteria

- [ ] MCPFormatter inherits FormatterBase
- [ ] Outputs valid JSON array
- [ ] Each file entry has: file_path, language, best_score, total_chunks
- [ ] Each file entry has chunks array with ALL chunks (after filtering)
- [ ] Each chunk has: content, line_range, score, is_head
- [ ] Respects min_score parameter (default 0.5)
- [ ] Files ordered by best chunk score (descending)
- [ ] 10+ unit tests pass with >90% coverage
- [ ] BDD Scenario 2 passes (3/3 total ✅)
- [ ] ALL 3 BDD smoke tests pass ✅
- [ ] No regressions in full test suite
- [ ] No mypy errors

---

**Result**: ALL formatters complete with unit test coverage, file grouping works across all formats, 3 BDD smoke tests verify integration ✅
