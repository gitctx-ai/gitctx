# TASK-0001.4.3.4: Update VerboseFormatter with Line Order + Best-Match Indicator (TDD - Unit Tests Only)

**Parent Story**: [STORY-0001.4.3](README.md)
**Status**: 🔵 Not Started
**Estimated Hours**: 3
**Actual Hours**: -

## Objective

Modify VerboseFormatter to inherit from FormatterBase and display ALL chunks per file, sorted by line order ascending (natural reading order). Add comment-style best-match indicator (`# ⭐`) for the highest-scoring chunk (copy-pasteable). Chunks are filtered by score threshold and filter_mode via base class, but VerboseFormatter adds explicit line sorting and best-chunk identification.

**Focus**: Unit tests only (12+ tests). BDD smoke tests remain unchanged (2/3 passing).

## BDD Progress

**Before this task**: 2/3 scenarios passing (foundation ready)
**After this task**: 2/3 scenarios passing (verbose format tested via unit tests, not BDD)

## Prerequisites

**Must complete before starting this task:**
- ✅ [TASK-0001.4.3.2](TASK-0001.4.3.2.md) - FormatterBase must exist for inheritance
- ✅ [TASK-0001.4.3.2](TASK-0001.4.3.2.md) - SearchResult.score property must exist
- ✅ [TASK-0001.4.3.2](TASK-0001.4.3.2.md) - CLI --filter flag must exist

**Can work in parallel with:**
- TASK-0001.4.3.3 (TerseFormatter - independent formatter)
- TASK-0001.4.3.5 (MCPFormatter - independent formatter)

## Implementation Checklist

### Unit Tests First (TDD - RED)

- [ ] Create or modify `tests/unit/formatters/test_verbose.py`
- [ ] Write test: `test_verbose_shows_all_chunks_per_file()`
  - [ ] Mock 3 SearchResult objects for same file
  - [ ] Verify 3 "Lines X-Y (score:...)" headers in output
- [ ] Write test: `test_verbose_file_header_shows_chunk_count()`
  - [ ] Mock 3 chunks for same file
  - [ ] Verify output contains "filename (3 chunks)"
- [ ] Write test: `test_verbose_singular_chunk_count()`
  - [ ] Mock 1 SearchResult object for file
  - [ ] Verify output contains "filename (1 chunk)" (singular, not "chunks")
- [ ] Write test: `test_verbose_chunks_sorted_by_line_order()`
  - [ ] Mock chunks with lines: 67, 45, 103
  - [ ] Verify output order: 45, 67, 103 (line ascending)
- [ ] Write test: `test_verbose_tie_breaking_with_equal_lines()`
  - [ ] Mock 3 chunks: same line (10), different scores (0.7, 0.95, 0.5)
  - [ ] Verify output order by score desc: 0.95, 0.7, 0.5
- [ ] Write test: `test_verbose_tie_breaking_with_equal_lines_and_scores()`
  - [ ] Mock 3 chunks: same line (10), same score (0.8), different content ("ccc", "aaa", "bbb")
  - [ ] Verify output order by content: "aaa", "bbb", "ccc" (lexicographic)
- [ ] Write test: `test_verbose_best_chunk_tie_breaking()`
  - [ ] Mock 3 chunks: same score (0.9), different lines (30, 10, 20)
  - [ ] Verify best-match indicator (⭐) appears on line 10 (earliest line wins tie)
  - [ ] Tests deterministic best chunk selection
- [ ] Write test: `test_verbose_preserves_syntax_highlighting()`
  - [ ] Mock Python code chunk
  - [ ] Verify output contains Rich markup or ANSI codes
  - [ ] Check for Syntax class usage
- [ ] Write test: `test_verbose_respects_min_score_parameter()`
  - [ ] Mock 3 chunks: scores 0.95, 0.60, 0.25
  - [ ] Call format with min_score=0.5
  - [ ] Verify only 2 chunks shown
- [ ] Write test: `test_verbose_shows_line_ranges_and_scores()`
  - [ ] Mock chunk: start=10, end=20, score=0.85
  - [ ] Verify output: "Lines 10-20 (score: 0.85)"
- [ ] Run tests: `uv run pytest tests/unit/formatters/test_verbose.py -v`
- [ ] All tests fail ✓

### Implementation (GREEN)

- [ ] Open `src/gitctx/cli/symbols.py`
- [ ] Add "best_match" symbol to both SYMBOLS dictionaries:
  - [ ] Legacy Windows: `"best_match": "*"`
  - [ ] Modern terminals: `"best_match": "⭐"`
- [ ] Open `src/gitctx/formatters/verbose.py`
- [ ] Add import: `from gitctx.formatters.base import FormatterBase`
- [ ] Add import: `from gitctx.cli.symbols import SYMBOLS`
- [ ] Change class declaration: `class VerboseFormatter(FormatterBase):`
- [ ] Modify `format()` method:
  - [ ] Extract min_similarity: `min_similarity = kwargs.get('min_similarity', 0.5)`
  - [ ] Extract filter_mode: `filter_mode = kwargs.get('filter', 'head')`
  - [ ] Call base class: `grouped = self._filter_and_group(results, min_similarity, filter_mode)`
  - [ ] Loop over grouped items: `for file_path, chunks in grouped.items():`
  - [ ] Identify best chunk (3-level deterministic tie-breaking): `best_chunk = max(chunks, key=lambda c: (c.score, -c.start_line, c.content))`
  - [ ] Sort by line order with tie-breaking: `chunks.sort(key=lambda c: (c.start_line, -c.score, c.content))`
  - [ ] Handle singular/plural: `chunk_word = "chunk" if len(chunks) == 1 else "chunks"`
  - [ ] Print file header: `console.print(f"\n[bold]{file_path}[/bold] ({len(chunks)} {chunk_word})")`
  - [ ] Loop over ALL chunks: `for chunk in chunks:`
  - [ ] Print chunk header with best-match indicator:
    - [ ] `if chunk == best_chunk: console.print(f"\n# {SYMBOLS['best_match']} Lines {chunk.start_line}-{chunk.end_line} (score: {chunk.score:.2f}, best match)")`
    - [ ] `else: console.print(f"\n# Lines {chunk.start_line}-{chunk.end_line} (score: {chunk.score:.2f})")`
  - [ ] Apply syntax highlighting: `highlighted = self._highlight_code(chunk.content, chunk.language)`
  - [ ] Print highlighted code: `console.print(highlighted)`
- [ ] Run unit tests: `uv run pytest tests/unit/formatters/test_verbose.py -v`
- [ ] All tests pass ✓

### Refactor (if needed)

- [ ] Check for code duplication
- [ ] Verify existing _highlight_code() method still works
- [ ] Ensure type hints are correct
- [ ] Run mypy: `uv run mypy src/gitctx/formatters/verbose.py`

### Verification

- [ ] Run unit tests: all pass (12+ tests)
- [ ] Run BDD scenarios: still 2/3 passing (unchanged - smoke tests already pass)
- [ ] Check code coverage: `uv run pytest tests/unit/formatters/test_verbose.py --cov=src/gitctx/formatters/verbose`
- [ ] Coverage >90%
- [ ] Run mypy: no errors
- [ ] Manually test: verify verbose output shows all chunks with colors
- [ ] Commit: `feat(TASK-0001.4.3.4): Add line sorting + best-match indicator to VerboseFormatter (2/3 BDD passing)`

## Files to Create/Modify

- **MODIFY**: `src/gitctx/formatters/verbose.py` (~25 lines changed)
  - Add FormatterBase inheritance
  - Update format() to loop over ALL chunks (not just best)
  - Pass min_score to base class
  - Preserve existing syntax highlighting logic

- **CREATE/MODIFY**: `tests/unit/formatters/test_verbose.py` (~180 lines)
  - 12+ unit tests covering multi-chunk display, line sorting, best-match indicator
  - Mock SearchResult fixtures
  - Verify syntax highlighting preserved
  - Tests for singular/plural, tie-breaking, best-chunk identification

## Pattern Reuse

**Use these existing patterns to avoid code duplication and maintain consistency:**

- **mock_search_result_factory**: Pytest fixture for creating SearchResult test objects (from tests/unit/conftest.py:962)
  - Usage: `result = mock_search_result_factory(start_line=15, end_line=25, hybrid_score=0.95, language="python")`
  - Provides sensible defaults for all fields, customize as needed
  - Ensures consistency across all formatter tests
- **FormatterBase._group_by_file()**: Centralized grouping and filtering
- **Existing Rich Syntax**: Already in VerboseFormatter, preserve it
- **strip_ansi() helper**: For ANSI code detection in tests
- **Score parsing**: Regex pattern `r'score: ([\d.]+)'`

## Performance Targets

- Formatting should be O(n) where n = number of results
- Syntax highlighting uses existing Rich/Pygments (no perf regression)

## Success Criteria

- [ ] VerboseFormatter inherits FormatterBase
- [ ] Shows file header with chunk count (after filtering)
- [ ] Shows ALL chunks per file (not just best)
- [ ] Each chunk has: Lines X-Y (score: Z.ZZ) + highlighted code
- [ ] Syntax highlighting preserved (uses existing _highlight_code())
- [ ] Respects min_score parameter (default 0.5)
- [ ] Chunks sorted by score descending within each file
- [ ] 6+ unit tests pass with >90% coverage
- [ ] BDD Scenario 4 passes (5/6 total)
- [ ] BDD Scenarios 5-6 still fail as expected
- [ ] No mypy errors

---

**Result**: Verbose format complete with file grouping, all chunks shown, syntax highlighting preserved
