# TASK-0001.4.3.4: Update VerboseFormatter with Line Order + Best-Match Indicator (TDD - Unit Tests Only)

**Parent Story**: [STORY-0001.4.3](README.md)
**Status**: 🟢 Complete
**Estimated Hours**: 3
**Actual Hours**: 4.5

## Objective

Modify VerboseFormatter to use composition pattern (filter_and_group_results function) and display ALL chunks per file, sorted by line order ascending (natural reading order). Add comment-style best-match indicator (`# ⭐`) for the highest-scoring chunk (copy-pasteable). Chunks are filtered by score threshold and filter_mode via shared function, but VerboseFormatter adds explicit line sorting and best-chunk identification.

**Focus**: Unit tests only (14 tests). BDD smoke tests will be updated in TASK-0001.4.3.5.

## BDD Progress

**Before this task**: 2/3 scenarios passing (foundation ready)
**After this task**: 2/3 scenarios passing (verbose format tested via unit tests, not BDD)

**Note**: E2E/CLI tests will be updated in TASK-0001.4.3.5 (Integration + E2E test updates)

## Prerequisites

**Must complete before starting this task:**
- ✅ [TASK-0001.4.3.2](TASK-0001.4.3.2.md) - FormatterBase converted to function
- ✅ [TASK-0001.4.3.2](TASK-0001.4.3.2.md) - SearchResult.score property exists
- ✅ [TASK-0001.4.3.2](TASK-0001.4.3.2.md) - CLI --filter flag exists

**Can work in parallel with:**
- TASK-0001.4.3.3 (TerseFormatter - independent formatter)
- TASK-0001.4.3.5 (MCPFormatter - independent formatter)

## Implementation Checklist

### Unit Tests First (TDD - RED)

- [x] Create or modify `tests/unit/formatters/test_verbose.py`
- [x] Write 14 comprehensive tests covering:
  - [x] File grouping with multiple chunks
  - [x] Line order sorting (not score order)
  - [x] Best-match indicator (⭐ or *)
  - [x] Tiebreaker logic (score ties broken by earliest line)
  - [x] Min similarity filtering
  - [x] Filter mode support (head/history/all)
  - [x] Multiple files sorted by best chunk
  - [x] Empty results handling
  - [x] Single chunk per file
  - [x] Platform-aware symbols
  - [x] Syntax highlighting preservation
- [x] Run tests: `uv run pytest tests/unit/formatters/test_verbose_new.py -v`
- [x] All 14 tests fail ✓ (RED phase complete)

### Implementation (GREEN)

- [x] Add best_match symbol to `src/gitctx/cli/symbols.py`:
  - [x] Legacy Windows: `"best_match": "*"`
  - [x] Modern terminals: `"best_match": "⭐"`
- [x] Update `src/gitctx/formatters/verbose.py`:
  - [x] Import filter_and_group_results, format_distance_score
  - [x] Import SYMBOLS for best-match indicator
  - [x] Convert dicts to Result dataclass with .score property
  - [x] Call filter_and_group_results() for filtering/grouping
  - [x] Sort chunks by line order: (start_line, -score, content)
  - [x] Identify best chunk: max by (score, -start_line, content)
  - [x] Add file header with chunk count (singular/plural)
  - [x] Add chunk headers with best-match indicator
  - [x] Preserve syntax highlighting with indentation
- [x] Update `tests/unit/formatters/test_base.py`:
  - [x] Remove FormatterBase class references
  - [x] Update imports to use filter_and_group_results function
  - [x] Update all test calls to use standalone function
- [x] Run tests: `uv run pytest tests/unit/formatters/test_verbose.py -v`
- [x] All 14 tests pass ✓ (GREEN phase complete)

### Refactor (if needed)

- [x] Moved imports to top-level (ruff compliance)
- [x] Fixed nested if statements (ruff SIM102)
- [x] Updated symbols test to include best_match
- [x] All quality gates pass
- [x] Run mypy: `uv run mypy src`
- [x] No errors ✓

### Verification

- [x] Run unit tests: all formatter tests pass (74/74)
- [x] Run all unit tests: 348 passing
- [x] Check code coverage: 97.34% (exceeds 85% requirement)
- [x] Run linting: ruff check passes
- [x] Run formatting: ruff format passes
- [x] Run mypy: no errors
- [x] BDD Scenarios: 2/3 still passing (unchanged as expected)

**Note**: E2E/CLI tests show expected failures (12 tests) due to verbose output format changes. These will be fixed in TASK-0001.4.3.5.

## Files Modified

- **src/gitctx/cli/symbols.py** (+1 line)
  - Added best_match symbol for modern and legacy terminals

- **src/gitctx/formatters/verbose.py** (~100 lines rewritten)
  - Uses composition (filter_and_group_results) instead of inheritance
  - Groups chunks by file with headers
  - Sorts chunks by line order (natural reading)
  - Adds best-match indicator (⭐) for highest-scoring chunk
  - Preserves syntax highlighting with indentation

- **tests/unit/formatters/test_verbose.py** (~500 lines)
  - Replaced old tests with 14 comprehensive new tests
  - Tests file grouping, line sorting, best-match indicator
  - Tests filtering (min_similarity, filter modes)
  - Tests platform-aware symbols
  - All tests use mock_search_result_factory for consistency

- **tests/unit/formatters/test_base.py** (~30 lines)
  - Updated to use filter_and_group_results function
  - Removed FormatterBase class references
  - All 12 tests still pass

- **tests/unit/cli/test_symbols.py** (+1 line)
  - Added best_match to required_keys set

## Pattern Reuse

**Used these existing patterns:**

- **mock_search_result_factory**: Created consistent test data (from tests/unit/conftest.py:962)
- **filter_and_group_results()**: Shared filtering/grouping logic (from formatters/base.py)
- **format_distance_score()**: Score formatting with precision (from formatters/base.py)
- **SYMBOLS dict**: Platform-aware symbol rendering (from cli/symbols.py)
- **Rich Syntax**: Preserved existing syntax highlighting with line numbers

## Success Criteria

- [x] VerboseFormatter uses composition (filter_and_group_results function)
- [x] Shows file header with chunk count (after filtering)
- [x] Shows ALL chunks per file (not just best)
- [x] Each chunk has: Lines X-Y (score: Z.ZZ, [best match]) + highlighted code
- [x] Best-match indicator (⭐ or *) on highest-scoring chunk
- [x] Syntax highlighting preserved with indentation
- [x] Respects min_similarity parameter (default 0.5)
- [x] Respects filter mode (head/history/all, default head)
- [x] Chunks sorted by line order ascending (natural reading)
- [x] 14 unit tests pass with 97.34% coverage (>90% requirement)
- [x] No ruff errors
- [x] No mypy errors
- [x] All formatter unit tests pass (74/74)

---

**Result**: VerboseFormatter complete with file grouping, line order sorting, best-match indicator, and composition pattern. Ready for E2E test updates in TASK-0001.4.3.5.
