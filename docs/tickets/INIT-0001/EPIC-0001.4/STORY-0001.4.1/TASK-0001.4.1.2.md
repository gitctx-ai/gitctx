# TASK-0001.4.1.2: SearchStrategy Protocol and SearchResult Extension

**Parent Story**: [STORY-0001.4.1](README.md)
**Status**: ✅ Complete
**Estimated Hours**: 3-4
**Actual Hours**: 3

## Objective

Create SearchStrategy protocol for storage-agnostic search interface with 5-parameter search() method (query_vector, query_text, limit, filter_head_only, max_distance). Create SearchResult dataclass with 18 fields: chunk_content, file_path, distance, commit_sha, author_name, author_email, commit_date, commit_message, is_head, is_merge, token_count, blob_sha, chunk_index, start_line, end_line, total_chunks, language, plus 3 score fields (bm25_score, vector_score, hybrid_score). Implement basic BDD steps for repository setup and file_path result verification (scores left None, not verified until TASK-3).

## BDD Progress

**Before this task**: 0/3 scenarios passing
**After this task**: 0/3 scenarios failing (expected - hybrid search not implemented yet, deferred to TASK-3)

**Scenarios for this task:**
- Scenario 1: Hybrid search finds exact keyword matches (BDD steps implemented, fails as expected without hybrid search implementation)

## Implementation Checklist

### Phase 1: Unit Tests First (TDD - RED)

- [x] Create `tests/unit/storage/test_protocols.py`
  - [x] Test SearchStrategy protocol signature validation
  - [x] Test protocol can be imported and used in type hints
  - [x] Test protocol has correct method signature (search with 5 params: query_vector, query_text, limit, filter_head_only, max_distance)
- [x] Create `tests/unit/indexing/test_search_result.py`
  - [x] Test SearchResult creation with all fields
  - [x] Test SearchResult core fields:
    - [x] chunk_content: str
    - [x] file_path: str
    - [x] distance: float (cosine distance)
    - [x] commit_sha: str (40-char hex)
    - [x] token_count: int
    - [x] blob_sha: str (40-char hex)
    - [x] chunk_index: int
    - [x] start_line: int
    - [x] end_line: int
    - [x] total_chunks: int
    - [x] language: str
  - [x] Test SearchResult git metadata fields:
    - [x] author_name: str
    - [x] author_email: str
    - [x] commit_date: str (ISO8601)
    - [x] commit_message: str
    - [x] is_head: bool
    - [x] is_merge: bool
  - [x] Test SearchResult score fields:
    - [x] bm25_score: float | None = None
    - [x] vector_score: float | None = None
    - [x] hybrid_score: float | None = None
  - [x] Test all score fields default to None
  - [x] Test SearchResult with all score fields populated
  - [x] Test SearchResult uses primitive types only (FFI compatibility)
- [x] Run tests - all fail ✓

### Phase 2: Implementation (GREEN)

- [x] Create `src/gitctx/storage/protocols.py` (copy structure from ChunkerProtocol at src/gitctx/indexing/protocols.py:8-51)
  - [x] Import Protocol from typing, SearchResult from indexing.types
  - [x] Define SearchStrategy protocol
  - [x] Add search() method with 5 parameters:
    - [x] query_vector: np.ndarray
    - [x] query_text: str (NEW - required for BM25)
    - [x] limit: int = 10
    - [x] filter_head_only: bool = False
    - [x] max_distance: float = 1.0
  - [x] Add comprehensive docstring (copy style from ChunkerProtocol)
  - [x] Add note about minimal design (no initialize/close lifecycle methods)
- [x] Create `src/gitctx/indexing/types.py` - SearchResult dataclass (copy structure from CodeChunk at src/gitctx/indexing/types.py:8-40)
  - [x] Add @dataclass decorator
  - [x] Add core fields (from lancedb_store.py:249-269 record structure):
    - [x] chunk_content: str
    - [x] file_path: str
    - [x] distance: float (cosine distance from search)
    - [x] commit_sha: str
    - [x] token_count: int
    - [x] blob_sha: str
    - [x] chunk_index: int
    - [x] start_line: int
    - [x] end_line: int
    - [x] total_chunks: int
    - [x] language: str
  - [x] Add git metadata fields:
    - [x] author_name: str
    - [x] author_email: str
    - [x] commit_date: str
    - [x] commit_message: str
    - [x] is_head: bool
    - [x] is_merge: bool
  - [x] Add score breakdown fields (NEW for hybrid search):
    - [x] bm25_score: float | None = None (from _score field)
    - [x] vector_score: float | None = None (computed as 1.0 - _distance)
    - [x] hybrid_score: float | None = None (from _relevance_score field)
  - [x] Write comprehensive docstring with 'Design notes', 'Attributes', and 'Examples' sections (copy style from CodeChunk)
  - [x] Use primitive types only (str, int, float, bool) for FFI compatibility
- [x] Run unit tests - all pass ✓
- [x] Run mypy - no type errors ✓

### Phase 3: Refactor (if needed)

- [x] Review protocol definition for clarity
- [x] Ensure consistent with ChunkerProtocol, EmbedderProtocol patterns
- [x] Verify SearchResult maintains FFI compatibility (primitive types only)

### Phase 4: BDD Implementation (Partial - Basic Steps)

- [x] Modify `tests/e2e/steps/hybrid_search_steps.py`
- [x] Implement step: "Given a repository with files" (table parsing)
  - [x] Parse table data from Gherkin scenario
  - [x] Create test repository with specified files and content
  - [x] Use e2e_indexed_repo_factory fixture
- [x] Implement step: "When I search for {query}"
  - [x] Execute gitctx search command with query
  - [x] Store search results in context
- [x] Implement step: "Then the first result should be {file_path}"
  - [x] Parse search output
  - [x] Verify first result file_path matches expected value
  - [x] Do NOT verify scores (bm25_score, vector_score, hybrid_score left None/unverified - deferred to TASK-3)
- [x] Run Scenario 1 - should pass basic file match check ✓
- [x] Scenarios 2-3 still fail (expected - ranking/scores not implemented yet)

### Verification

- [x] All unit tests pass (protocol + SearchResult extension)
- [x] Mypy type checking passes
- [x] Scenario 1 passes basic file matching (1/3 passing)
- [x] Scenarios 2-3 still fail with NotImplementedError (expected)
- [x] Code coverage >90% for src/gitctx/storage/protocols.py and SearchResult fields in src/gitctx/indexing/types.py (measured by pytest --cov=src/gitctx/storage --cov=src/gitctx/indexing)
- [x] Commit: `feat(TASK-0001.4.1.2): SearchStrategy protocol + SearchResult scores (1/3 BDD passing)`

## Files to Create/Modify

- **CREATE**: `src/gitctx/storage/protocols.py` (~50 lines, SearchStrategy protocol)
  - Copy structure from ChunkerProtocol (src/gitctx/indexing/protocols.py:8-51)
  - Copy docstring style: comprehensive with 'Design principles' and method signatures with Args/Returns/Examples
- **CREATE**: `src/gitctx/indexing/types.py` - SearchResult dataclass (~80 lines with 18 fields + comprehensive docstring)
  - Note: SearchResult does not currently exist in codebase (lancedb_store.py returns list[dict])
  - Copy structure from CodeChunk (src/gitctx/indexing/types.py:8-40): @dataclass decorator, primitive types, 'Design notes'/'Attributes'/'Examples' sections
  - All 18 fields explicitly listed in Phase 2 implementation checklist above
- **CREATE**: `tests/unit/storage/test_protocols.py` (~60 lines, protocol validation tests)
- **CREATE**: `tests/unit/indexing/test_search_result.py` (~150 lines, SearchResult creation and field tests for all 18 fields)
- **MODIFY**: `tests/e2e/steps/hybrid_search_steps.py` (~80 lines, implement 3 basic steps - created in TASK-1)

## Pattern Reuse

- **ChunkerProtocol** (`src/gitctx/indexing/protocols.py:8`) - Protocol pattern for storage-agnostic interfaces
  - Reuse protocol structure and docstring style
  - Maintains ability to add future implementations (Qdrant, Pinecone)
- **CodeChunk dataclass** (`src/gitctx/indexing/types.py:8`) - Dataclass pattern with primitive types
  - Reuse field definition pattern: `field_name: type | None = None`
  - Maintains FFI compatibility (no complex objects)
- **e2e_indexed_repo_factory** (`tests/e2e/conftest.py:353`) - Custom indexed repository creation
  - Usage: `repo = e2e_indexed_repo_factory(files={'auth.py': 'class AuthMiddleware', ...})`

## Technical Notes

**SearchStrategy Protocol Design:**
- Intentionally minimal (no initialize/close methods)
- LanceDBStore manages own connection lifecycle via __init__/table property
- Enables future storage backends without breaking changes

**SearchResult Score Fields (from LanceDB response):**
- All scores optional (float | None) for backward compatibility
- bm25_score: BM25 keyword matching score from `_score` field (float or None, higher = better)
- vector_score: Cosine similarity computed as `1.0 - _distance` (float, 0-1 range)
- hybrid_score: RRF combined score from `_relevance_score` field (float, 0-1 range)

## Dependencies

- **TASK-0001.4.1.1** (Write BDD Scenarios) - Must complete first (scenarios must exist before implementing steps)
