# TASK-0001.4.1.1: Write BDD Scenarios for Hybrid Search

**Parent Story**: [STORY-0001.4.1](README.md)
**Status**: ✅ Complete
**Estimated Hours**: 2-3
**Actual Hours**: 1

## Objective

Write ALL 3 BDD scenarios for hybrid search (keyword match, semantic match, combined ranking) with stubbed step definitions. All scenarios should fail with NotImplementedError (expected). This defines WHAT the system should do before HOW it does it.

## BDD Progress

**Before this task**: 0/3 scenarios exist
**After this task**: 0/3 scenarios passing (all stubbed, all failing 🔴)

**Scenarios for this task:**
- Scenario 1: Hybrid search finds exact keyword matches
- Scenario 2: Hybrid search finds semantic matches
- Scenario 3: Hybrid search combines keyword and semantic ranking

## Implementation Checklist

### Phase 1: Create Feature File

- [x] Create `tests/e2e/features/hybrid_search.feature`
- [x] Write Scenario 1: "Hybrid search finds exact keyword matches"
  - [x] Given repository with auth/middleware.py and auth/handlers.py
  - [x] When search for "AuthMiddleware"
  - [x] Then first result should be "src/auth/middleware.py"
  - [x] And result should have BM25 score > 0.7 (keyword match)
- [x] Write Scenario 2: "Hybrid search finds semantic matches"
  - [x] Given repository with auth files
  - [x] When search for "user authentication logic"
  - [x] Then results include both middleware.py and handlers.py
  - [x] And results should have vector scores > 0.7 (semantic match)
- [x] Write Scenario 3: "Hybrid search combines keyword and semantic ranking"
  - [x] Given repository with jwt.py, oauth.py, auth_guide.md
  - [x] When search for "JWT auth middleware class"
  - [x] Then jwt.py ranks first (keyword + semantic match)
  - [x] And oauth.py ranks second (semantic match)
  - [x] And auth_guide.md ranks lower (partial semantic match)

### Phase 2: Create Stubbed Step Definitions

- [x] Create `tests/e2e/steps/hybrid_search_steps.py`
- [x] Stub step: "Given I am in a git repository" (background)
- [x] Stub step: "Given a repository with files" (table parsing)
- [x] Stub step: "And the repository is indexed"
- [x] Stub step: "When I search for {query}"
- [x] Stub step: "Then the first result should be {file_path}"
- [x] Stub step: "And the result should have BM25 score > 0.7"
- [x] Stub step: "Then results should include both {file1} and {file2}"
- [x] Stub step: "And results should have vector scores > 0.7"
- [x] Stub step: "Then {file} should rank {position}"
- [x] Stub step: "And {file} should rank {position}"
- [x] All stubs raise NotImplementedError with message indicating which task implements

### Verification

- [x] Run `uv run pytest tests/e2e/test_hybrid_search_features.py -v`
- [x] Verify all 3 scenarios fail with NotImplementedError
- [x] Verify clear error messages showing which task implements each step
- [x] Create test runner file `tests/e2e/test_hybrid_search_features.py`
- [x] Commit: `test(TASK-0001.4.1.1): Write BDD scenarios for hybrid search (0/3 stubbed)`

## Files to Create

- **CREATE**: `tests/e2e/features/hybrid_search.feature` (~80 lines, 3 Gherkin scenarios)
- **CREATE**: `tests/e2e/steps/hybrid_search_steps.py` (~120 lines, 8 stubbed steps)

## Pattern Reuse

- **e2e_indexed_repo_factory** (`tests/e2e/conftest.py:353`) - For creating custom indexed repos with specific file structure
- **e2e_cli_runner** (`tests/e2e/conftest.py`) - For running gitctx search commands
- **Existing search_steps.py** (`tests/e2e/steps/search_steps.py`) - Pattern for Given/When/Then step structure

## Example Stub

```python
@then('the result should have BM25 score > 0.7')
def check_bm25_score(context):
    """Verify BM25 score indicates strong keyword match (> 0.7 threshold).

    Implementation: TASK-0001.4.1.3 (after hybrid search implemented)
    """
    raise NotImplementedError(
        'BM25 score verification not implemented yet. '
        'Will be implemented in TASK-0001.4.1.3 when hybrid search is working.'
    )
```

## Dependencies

**None** - This is TASK-1 in incremental BDD pattern, defines all scenarios upfront.
